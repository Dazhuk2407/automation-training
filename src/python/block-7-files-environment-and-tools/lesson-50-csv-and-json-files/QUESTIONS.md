# Питання для самоперевірки — Lesson 50: CSV and JSON Files

1. **Що повертає `csv.reader` — які типи мають рядок і його елементи?**
2. **Чим `csv.DictReader` зручніший за `csv.reader`?**
3. **Навіщо потрібен `newline=""` при відкритті CSV-файлу?**
4. **Що робить `writeheader()` у `csv.DictWriter` і що буде, якщо його не викликати?**
5. **У чому різниця між `json.load` і `json.loads`?**
6. **У чому різниця між `json.dump` і `json.dumps`?**
7. **Що робить параметр `indent=2` у `json.dump`?**
8. **Якого типу будуть числа, прочитані з CSV, і як їх перетворити?**
9. **Коли обрати CSV, а коли JSON для тестових даних?**
10. **Де у реальному QA automation використовують читання CSV та запис JSON?**

---

**Далі:** `lesson-51-pathlib`
