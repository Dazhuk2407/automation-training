# Питання для самоперевірки — Lesson 62: Helper Scripts

1. **Що таке helper-функція і чому вона має бути «чистою»?**
2. **Що робить `chunk(seq, n)` і яким буде останній шматок, якщо довжина не ділиться на `n`?**
3. **На скільки рівнів розгортає список `flatten`?**
4. **Як реалізувати `retry(func, attempts)` без `time.sleep`?**
5. **Чому `time.sleep` у юніт-тестах — це погано?**
6. **Що повертає `safe_get(d, *keys)`, якщо ключ відсутній?**
7. **Чому важлива обробка винятків у `retry` і що робити після вичерпання спроб?**
8. **Що означає «одна функція — одна відповідальність»?**
9. **Чому прихований стан / побічні ефекти ускладнюють тестування?**
10. **Де у реальному QA automation застосовуються `chunk`, `retry`, `safe_get`?**

---

**Далі:** `lesson-63-api-automation-intro`
