# Питання для самоперевірки — Lesson 63: API Automation Intro

1. **Що таке API-автоматизація і чим вона краща за UI-тести?**
2. **Які основні HTTP методи та за що кожен відповідає?** (GET/POST/PUT/PATCH/DELETE)
3. **Що таке ідемпотентність і які методи ідемпотентні?**
4. **Як класифікувати статус-код за першою цифрою?** (2xx/3xx/4xx/5xx)
5. **Що означають коди 200, 201, 400, 401, 404, 500?**
6. **Навіщо заголовок `Content-Type` і навіщо `Authorization`?**
7. **Чому токен НІКОЛИ не можна хардкодити і звідки його читати?** (`os.getenv`)
8. **Що робить бібліотека `requests` (оглядово)?** (`.status_code`, `.json()`)
9. **Опишіть будову API-тесту: arrange → act → assert.**
10. **Чому недостатньо перевіряти лише статус-код у тесті?**

---

**🎉 Вітаємо — ви завершили курс Python for QA Automation!**
