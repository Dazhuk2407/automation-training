# Питання для самоперевірки — Lesson 18: if / else

## 📋 Основи

1. **Яка різниця між `if/else` та `if/elif/else`?**
   - Коли потрібен elif?
   - Що станеться без elif якщо є кілька умов?

2. **Що таке truthy та falsy?**
   - Назвіть 5 falsy значень
   - Чому `if users:` краще за `if len(users) > 0:`?

3. **Який пріоритет у `not`, `and`, `or`?**
   - Що означає `a or b and c`?
   - Як змінити пріоритет?

## 🔧 Практика

4. **Чому `if value is None` краще за `if value == None`?**

5. **Що таке early return?**
   - Чому він кращий за глибоку вкладеність?
   - Покажіть приклад

6. **Як перевірити boundary values?**
   - Якщо `< 100` → "fast", `< 500` → "normal"
   - Що поверне функція для 100?

## 🧪 У тестах

7. **Як тестувати функцію з кількома гілками if/elif/else?**
   - Скільки тестів мінімально потрібно?
   - Чи потрібно тестувати boundary values?

8. **Чому кожна гілка умови потребує окремого тесту?**

9. **Як перевірити що валідація повертає правильну помилку?**

---

**Далі:** `lesson-19-conditional-expressions`