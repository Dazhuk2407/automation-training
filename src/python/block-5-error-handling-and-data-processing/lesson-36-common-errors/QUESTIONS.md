# Питання для самоперевірки — Lesson 36: Common Python Errors

1. **Чим `ValueError` відрізняється від `TypeError`?** (значення vs тип)
2. **Що поверне `int("abc")` і чому?**
3. **Який виняток кине `"a" + 1` і як це виправити?**
4. **Що таке `KeyError` і яка безпечна альтернатива `dict[key]`?**
5. **Коли виникає `IndexError`?** Чи впаде `list[-1]` на непорожньому списку?
6. **Чому `None.foo()` кидає `AttributeError`?**
7. **Що зазвичай спричиняє `NameError`?**
8. **Як безпечно уникнути `ZeroDivisionError`?**
9. **Чому погано писати `except Exception` замість конкретного винятку?**
10. **Як за допомогою `pytest.raises` перевірити, що функція кидає потрібний виняток?**

---

**Далі:** `lesson-37-reading-tracebacks`
