# Питання для самоперевірки — Lesson 47: Python OOP Concepts

1. **Назвіть 4 стовпи ООП.** (encapsulation, inheritance, polymorphism, abstraction)
2. **Чим `_single` відрізняється від `__double` підкреслення?**
3. **Що таке name mangling і в що перетворюється `self.__pin` у класі `Account`?** (`_Account__pin`)
4. **Чи можна дістатись до `__private` атрибута ззовні?** (так, через `_Class__attr`)
5. **Навіщо потрібен `@property`?**
6. **Що таке поліморфізм? Наведіть приклад із методом `.area()`.**
7. **Що таке duck typing?**
8. **Що таке абстракція і чим вона корисна?**
9. **Для чого `abstractmethod`?**
10. **Де у реальному QA automation використовується поліморфізм?** (Page-класи з `.is_loaded()`)

---

**Далі:** `lesson-48-magic-methods`
