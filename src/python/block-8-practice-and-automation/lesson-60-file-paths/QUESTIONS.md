# Питання для самоперевірки — Lesson 60: File Paths

1. **Чим абсолютний шлях відрізняється від відносного?**
2. **Як перевірити, що шлях абсолютний?** (`Path(p).is_absolute()`)
3. **Що таке поточна робоча директорія (cwd) і як її отримати?** (`Path.cwd()`)
4. **Від чого рахуються відносні шляхи — від файлу скрипта чи від cwd?**
5. **Чому відносні шляхи ламаються при запуску з іншої теки або в CI?**
6. **Як побудувати шлях відносно скрипта?** (`Path(__file__).parent / ...`)
7. **Що робить `resolve()` і які сегменти прибирає?** (`..`, `.`)
8. **Чому шляхи краще з'єднувати через `/`, а не конкатенацією рядків?**
9. **Що таке path traversal і як від нього захиститися?**
10. **Як у QA automation правильно вказувати шлях до тест-даних?**

---

**Далі:** `lesson-61-processing-test-data`
