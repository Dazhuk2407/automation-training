# Питання для самоперевірки — Lesson 29: Return Values

1. **Що повертає функція без return?**
2. **Чим return відрізняється від print?**
3. **Як повернути кілька значень з функції?**
4. **Що таке early return і чому він кращий за глибоку вкладеність?**
5. **Чи виконується код після return?**
6. **Як розпакувати результат функції що повертає tuple?**
7. **Чому `result = func()` може бути None якщо забули return?**
8. **Коли повертати tuple, а коли dict?**

---

**Далі:** `lesson-30-args-kwargs`