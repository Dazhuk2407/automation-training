# Питання для самоперевірки — Lesson 52: os and sys

1. **Чим `os.getenv("KEY", default)` безпечніший за `os.environ["KEY"]`?**
2. **Що поверне `os.getenv("NOPE")`, якщо змінної немає і дефолт не заданий?**
3. **Навіщо у QA читати `BASE_URL` / `TIMEOUT` з env vars, а не хардкодити?**
4. **Чому не можна зберігати `API_TOKEN` як літерал у коді?**
5. **Що робить `os.path.splitext("report.txt")`?**
6. **Яка різниця між `os.path.basename` та `os.path.dirname`?**
7. **Чому для нового коду зручніший `pathlib`, а не `os.path`?**
8. **Що містить `sys.argv[0]` і з якого індексу починаються аргументи?**
9. **Що означає `sys.exit(0)` проти `sys.exit(1)`?**
10. **Як за допомогою `sys.platform` пропустити тест на Windows?**

---

**Далі:** `lesson-53-dates-and-time`
