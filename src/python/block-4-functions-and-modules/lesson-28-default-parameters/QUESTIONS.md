# Питання для самоперевірки — Lesson 28: Default Parameters

1. **Як задати значення за замовчуванням для параметра?**
2. **Чому `def func(a=1, b):` — SyntaxError?**
3. **Що станеться з `def add(item, items=[])` при повторних викликах?**
4. **Чому mutable default — це баг?**
5. **Як правильно задати default для list/dict?**
6. **Чим `None` як default краще за `[]`?**
7. **Як перевірити що параметр не передали?** (`if x is None:`)
8. **Чи можна мати кілька параметрів з default?**
9. **Чому ця пастка — одне з найпопулярніших питань на Python-співбесідах?**

---

**Далі:** `lesson-29-return-values`