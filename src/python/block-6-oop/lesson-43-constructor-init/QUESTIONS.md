# Питання для самоперевірки — Lesson 43: Constructor (`__init__`)

1. **Що таке `__init__` і коли він викликається?**
2. **Що означає перший параметр `self`?**
3. **Як передати параметри в об'єкт при його створенні?** (`User("Alice")`)
4. **Чим `self.name = name` відрізняється від `name = name`?**
5. **Як задати значення за замовчуванням для параметра `__init__`?**
6. **Що повертає `__init__`?** (`None`)
7. **Що станеться, якщо написати `return self` у `__init__`?**
8. **Чим об'єкт без `__init__` відрізняється від об'єкта з `__init__`?**
9. **Як обчислити похідний атрибут у `__init__`?** (напр. `self.total = passed + failed`)
10. **Чому небезпечно використовувати `def __init__(self, tests=[])` і як зробити правильно?**

---

**Далі:** `lesson-44-instance-and-class-methods`
