# Питання для самоперевірки — Lesson 45: Static Methods

1. **Що таке `@staticmethod` і чим він відрізняється від звичайного методу?**
2. **Які параметри НЕ отримує static method?** (`self`, `cls`)
3. **Якими двома способами можна викликати static method?** (`Class.method()`, `obj.method()`)
4. **Коли обирати static, а коли instance method?**
5. **Чому валідатор `is_valid_email` логічно робити static?**
6. **Чим static method відрізняється від class method?**
7. **Що станеться, якщо забути декоратор `@staticmethod`?**
8. **Чи має static method доступ до `self.attribute`?** (ні)
9. **Наведіть приклад static-методу для QA automation.** (валідатор, генератор id)
10. **Чому static method легко тестувати?**

---

**Далі:** `lesson-46-inheritance`
