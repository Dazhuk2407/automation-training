# Приклади — Lesson 40: Regular Expressions

- `example_1_search_match.py` — `search` / `match` / `fullmatch` та Match object
- `example_2_patterns.py` — метасимволи, квантифікатори, класи, `findall`, `sub`
- `example_3_groups_qa.py` — групи `()` та валідація/витяг у QA

```bash
pytest example_1_search_match.py -v
pytest example_2_patterns.py -v
pytest example_3_groups_qa.py -v
```
