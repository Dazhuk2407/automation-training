# Питання для самоперевірки — Lesson 56: pip and requirements

1. **Опишіть повний робочий цикл залежностей: install → freeze → відтворення.**
2. **Яка команда встановлює всі залежності з файлу?** (`pip install -r requirements.txt`)
3. **Яка команда фіксує поточні версії у файл?** (`pip freeze > requirements.txt`)
4. **Що означає специфікатор `==` і чим він відрізняється від `>=`?**
5. **Що дозволяє `~=1.4` і чого не дозволяє?**
6. **Як у `requirements.txt` записати коментар і чи ігноруються порожні рядки?**
7. **Навіщо розділяти `requirements.txt` та `requirements-dev.txt`?**
8. **Чому у QA-проєктах важливо пінити версії?**
9. **Чому не можна комітити `.venv/` замість `requirements.txt`?**
10. **Який інструмент перевіряє залежності на відомі вразливості?** (`pip-audit`)

---

**Далі:** `lesson-57-logging`
