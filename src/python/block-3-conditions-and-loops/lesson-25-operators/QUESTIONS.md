# Питання для самоперевірки — Lesson 25: Operators

1. **Чим `/` відрізняється від `//`?**
2. **Що повертає `10 % 3`?**
3. **Чому `is` не підходить для порівняння чисел?**
4. **Коли `is` потрібен?** (підказка: None)
5. **Який пріоритет у `not`, `and`, `or`?**
6. **Чим `in` для рядка відрізняється від `in` для списку?**
7. **Що таке ланцюжок порівнянь `1 < x < 10`?**
8. **Що означає `x += 5`?**
9. **Чому `if a or b and c:` може працювати не так як очікуєте?**
10. **Як перевірити що ключ є у словнику через `in`?**

---

**Вітаємо! Ви завершили Block 3.**