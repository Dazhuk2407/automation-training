# Питання для самоперевірки — Lesson 35: try / except / finally

1. **Навіщо потрібен `try/except`?** (щоб виняток не зупиняв програму)
2. **Що станеться з блоком `except`, якщо у `try` не було помилки?**
3. **Чому порядок `except` має бути від конкретного до загального?**
4. **Як отримати текст повідомлення винятку?** (`except E as e` + `str(e)`)
5. **Як перехопити кілька типів винятків одним `except`?** (`except (A, B):`)
6. **Коли виконується блок `else`?**
7. **Коли виконується блок `finally`?**
8. **Чим `else` відрізняється від коду після `try/except`?**
9. **Чому голий `except:` — погана практика?**
10. **Де у реальному QA automation корисно повертати дефолт при помилці?**

---

**Далі:** `lesson-36-common-errors`
