# Питання для самоперевірки — Lesson 24: Generators

1. **Чим generator expression відрізняється від list comprehension?**
2. **Що означає "ліниві обчислення"?**
3. **Чому генератор можна ітерувати тільки один раз?**
4. **Чим `yield` відрізняється від `return`?**
5. **Як `all()` та `any()` працюють з генераторами?**
6. **Коли генератор ефективніший за список?**
7. **Як перетворити генератор на список?**
8. **Чи можна використати `len()` на генераторі?**

---

**Далі:** `lesson-25-operators`