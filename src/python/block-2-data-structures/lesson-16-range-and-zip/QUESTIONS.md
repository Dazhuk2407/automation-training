# Питання для самоперевірки — Lesson 16: range() та zip()

## 📋 range()

1. **Що повертає `range(5)`?**
   - Це список чи ітератор?
   - Як перетворити на список?

2. **Як створити послідовність [2, 4, 6, 8]?**
   - Який range() для цього потрібен?

3. **Як створити зворотну послідовність [5, 4, 3, 2, 1]?**

## 🔗 zip()

4. **Що станеться якщо у zip() списки різної довжини?**
   - Які елементи будуть загублені?

5. **Як створити словник з двох списків через zip()?**
   - Назвіть конкретний синтаксис

6. **Яка різниця між `zip(a, b)` та `zip(b, a)`?**

## 🔢 enumerate()

7. **Навіщо enumerate() якщо є range(len())?**
   - Який варіант pythonic?

8. **Що робить параметр `start` в enumerate()?**

## 🧪 У тестах

9. **Як використати zip() для створення пар input/expected?**
10. **Як через enumerate() знайти індекси елементів що відповідають умові?**

---

**Далі:** `lesson-17-test-data-structures`