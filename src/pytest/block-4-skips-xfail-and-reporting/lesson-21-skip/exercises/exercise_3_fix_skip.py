"""
Вправа 3: Виправ пропуск (FIX).

Тест нижче ПОВИНЕН бути пропущений — фіча ще не готова.
Але зараз він виконується і ПАДАЄ (рівно 1 падаючий тест).

Крок 1: Запустіть файл — побачите один FAILED.
Крок 2: Виправте test_search_feature ОДНИМ зі способів:
        (а) додайте @pytest.mark.skip(reason="фіча ще не реалізована"), АБО
        (б) виправте assert так, щоб він відповідав реальному результату.
Крок 3: Заповніть блок ВІДПОВІДЬ унизу.

Після виправлення: 0 failures.

Запуск: pytest exercise_3_fix_skip.py -v
"""

import pytest  # noqa: F401 — знадобиться для @pytest.mark.skip


def search(query):
    """Пошук ще не реалізовано — завжди повертає порожній список."""
    return []


def test_addition_works():
    """Робочий тест — проходить."""
    assert 1 + 1 == 2


def test_search_feature():
    """ПАДАЄ: фіча пошуку ще не готова, а тест очікує результат.

    Цей тест мав би бути пропущений, поки search() не реалізовано.
    """
    results = search("pytest")
    assert len(results) == 3  # search() ще нічого не повертає -> падіння


def test_string_works():
    """Робочий тест — проходить."""
    assert "abc".upper() == "ABC"


# ВІДПОВІДЬ:
# Спосіб виправлення (skip чи fix assert): _______________
# Чому цей тест варто пропустити, а не видалити: _______________
# Що покаже звіт після виправлення (PASSED / SKIPPED): _______________
