# Питання для самоперевірки — Lesson 0: Знайомство з Pytest

Після вивчення цього уроку ви повинні вміти відповісти на ці питання:

## 🎯 Базові концепції

1. **Що таке Pytest?**
   - Для чого він потрібен?
   - Чому pytest простіший за unittest?
   - Наведіть приклад тесту на pytest vs unittest

2. **Як встановити Pytest?**
   - Яка команда для встановлення?
   - Як перевірити що pytest встановлено?

3. **Як pytest знаходить тести?**
   - Як має називатись тестовий файл?
   - Як має називатись тестова функція?
   - Що буде якщо назвати функцію `check_add()` замість `test_add()`?

## ✅ Assert

4. **Що таке assert?**
   - Як написати простий assert?
   - Що відбувається коли assert не пройде?
   - Назвіть 3 різні види assert (рівність, порівняння, membership)

5. **Перевірка винятків**
   - Як перевірити що функція кидає помилку?
   - Який синтаксис у `pytest.raises()`?
   - Як перевірити текст помилки через `match`?

## 🚀 Запуск тестів

6. **Як запустити тести?**
   - Команда для запуску всіх тестів
   - Команда для запуску конкретного файлу
   - Команда для запуску одного тесту з файлу

7. **Флаги при запуску**
   - Що дає `-v`?
   - Що дає `-s`?
   - Що дає `-x`?

## 💭 Концептуальні питання

8. **Чому тести мають бути простими?**
   - Що поганого в тесті який тестує 10 речей одночасно?

9. **Як читати вивід pytest?**
   - Що означає `PASSED`?
   - Що означає `FAILED`?
   - Як зрозуміти з виводу pytest що пішло не так?

10. **Навіщо тестувати винятки?**
    - Чому важливо перевіряти не тільки "щасливий шлях"?
    - Наведіть приклад коли тест на виняток корисний

---

**Далі:** `lesson-1-install-pytest` — встановлення та налаштування pytest