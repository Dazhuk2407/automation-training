# Питання для самоперевірки — Lesson 30: *args та **kwargs

1. **Що таке `*args` і який тип він створює?**
2. **Що таке `**kwargs` і який тип він створює?**
3. **Який порядок: regular, *args, **kwargs?**
4. **Як розпакувати list при виклику функції?** (`func(*my_list)`)
5. **Як розпакувати dict при виклику?** (`func(**my_dict)`)
6. **Навіщо потрібні функції-обгортки?**
7. **Чому `args` — tuple, а не list?**
8. **Як написати `assert_fields(data, **expected)` для тестів?**
9. **Чи можна мати `*args` та `**kwargs` одночасно?**
10. **Де у реальному QA automation використовується `**kwargs`?**

---

**Далі:** `lesson-31-lambda-functions`