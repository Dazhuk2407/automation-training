# Питання для самоперевірки — Lesson 23: List Comprehensions

1. **Як переписати `for + append` через comprehension?**
2. **Де ставиться `if` для фільтрації, а де для тернарного?**
3. **Чим `{k: v for ...}` відрізняється від `[x for ...]`?**
4. **Як створити set comprehension?**
5. **Коли comprehension погіршує читабельність?**
6. **Як через comprehension дістати всі id зі списку dict?**
7. **Що станеться з `[x if x > 0 for x in items]`?**
8. **Як відфільтрувати словник — залишити тільки truthy значення?**

---

**Далі:** `lesson-24-generators`