# Питання для самоперевірки — Lesson 40: Regular Expressions

1. **Навіщо потрібні regex у QA automation?** (пошук, валідація, витяг)
2. **Чим відрізняються `re.search`, `re.match` і `re.fullmatch`?**
3. **Що повертає функція, якщо збігу немає?** (`None`)
4. **Що означають метасимволи `\d`, `\w`, `\s`, `.`?**
5. **Чим відрізняються квантифікатори `+`, `*`, `?`, `{n}`?**
6. **Як задати клас символів `[a-z0-9]` і як зробити заперечення `[^...]`?**
7. **Що повертає `re.findall` без груп і що — з групами `()`?**
8. **Як витягти текст групи з Match object?** (`.group(1)`)
9. **Що робить `re.sub` і як замаскувати числа у лозі?**
10. **Чому патерни треба писати як raw string `r"..."`?**

---

**Далі:** `lesson-41-json-data`
