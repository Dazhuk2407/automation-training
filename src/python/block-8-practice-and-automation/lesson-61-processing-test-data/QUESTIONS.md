# Питання для самоперевірки — Lesson 61: Processing Test Data

1. **Чому лог-рядок зручно парсити у dict, а не працювати з ним як з текстом?**
2. **Чим `split()` без аргументів кращий за `split(" ")` для парсингу логів?**
3. **Для чого потрібен `partition` і чим він відрізняється від `split`?**
4. **Як порахувати кількість рядків із певним статусом?**
5. **Що поверне `pass_rate`, якщо список рядків порожній, і чому це важливо?**
6. **Як уникнути `ZeroDivisionError` при обчисленні pass rate?**
7. **Навіщо викликати `strip()` перед перевіркою статусу рядка?**
8. **Як згрупувати рядки у dict `status -> count`?**
9. **Чому підрахунок статусів варто робити регістронезалежно?**
10. **Де у реальному QA automation використовується перетворення логу у summary?**

---

**Далі:** `lesson-62-helper-scripts`
