# Питання для самоперевірки — Lesson 11: Dictionaries

## 📋 Основи

1. **Як створити порожній словник?**
   - Два способи
   - Чи зберігає словник порядок ключів?

2. **Що станеться при `user["email"]` якщо ключа `email` немає?**
   - Яка помилка виникне?
   - Як її уникнути?

3. **Що перевіряє `"name" in user`?**
   - Ключі чи значення?
   - Як перевірити наявність значення?

## 🔧 Методи

4. **Яка різниця між `del user["key"]` та `user.pop("key")`?**
   - Що повертає pop()?
   - Що станеться якщо ключа немає?

5. **Що робить `update()`?**
   - Як він працює з існуючими ключами?
   - Як з новими?

6. **Чим `keys()`, `values()` та `items()` відрізняються?**
   - Що повертає кожен?
   - Який найчастіше використовується в циклах?

## 🧪 У тестах

7. **Як перевірити що API response має всі обов'язкові поля?**
   - Через цикл по required fields
   - Яку перевірку використати?

8. **Чому `assert "Alice" in user` може не працювати як очікується?**
   - Що саме перевіряє `in` для dict?

9. **Як перевірити значення вкладеного словника?**
   - Наприклад: `response["data"]["users"][0]["name"]`
   - Які ризики у такому ланцюжку?

10. **Чому зміна словника в одному тесті може зламати інший?**
    - Як це пов'язано з mutable природою dict?
    - Як уникнути?

---

**Далі:** `lesson-12-safe-dict-access`