# Питання для самоперевірки — Lesson 42: Classes and Objects

1. **У чому різниця між класом і об'єктом (екземпляром)?**
2. **Яким ключовим словом оголошують клас?**
3. **Як створити екземпляр класу `Dog`?** (`d = Dog()`)
4. **Що таке атрибут екземпляра і як до нього звернутися?**
5. **Що таке метод класу?**
6. **Навіщо потрібен перший параметр `self`?**
7. **На що вказує `self` під час виклику `obj.method()`?**
8. **Що станеться, якщо забути `self` у визначенні методу?**
9. **Чи мають різні екземпляри одного класу спільний чи окремий стан?**
10. **Де у реальному QA automation зручно застосувати клас-обгортку (напр. `ApiClient`)?**

---

**Далі:** `lesson-43-constructor-init`
