# Питання для самоперевірки — Lesson 7: Запуск тестів з CLI

## 🚀 Базовий запуск

1. **Як запустити всі тести в проєкті?**
   - А якщо тести лежать у папці `tests/`?

2. **Як запустити один конкретний тест з файлу?**
   - Який формат шляху?
   - А якщо тест у класі?

3. **Яка різниця між `-v` та `-q`?**
   - Що показує кожен?
   - Коли який зручніший?

## 📊 Вивід

4. **Чому print() не видно у виводі pytest за замовчуванням?**
   - Яка опція це виправляє?
   - Чому pytest приховує stdout?

5. **Яка різниця між `--tb=short`, `--tb=long` та `--tb=no`?**
   - Коли який рівень корисний?

6. **Що показує `--collect-only`?**
   - Чи запускає він тести?
   - Коли ви використаєте цю команду?

## 🔍 Фільтрація

7. **Як `-k` шукає тести?**
   - Чи шукає він тільки в назві функції, чи в повному шляху?
   - Як виключити тести з певним словом?
   - Як комбінувати `or` та `and`?

8. **Яка різниця між `-k "login"` та `-m login`?**
   - `-k` шукає в _______
   - `-m` фільтрує за _______

## 🛑 Контроль виконання

9. **Коли використовувати `-x`?**
   - Що станеться з рештою тестів?
   - Чим `-x` відрізняється від `--maxfail=1`?

10. **Що роблять `--lf` та `--ff`?**
    - Як pytest знає які тести впали?
    - Коли це корисно?

## 📦 Плагіни

11. **Які з цих команд потребують окремого плагіна?**
    - `pytest -v`
    - `pytest --cov=src`
    - `pytest -n auto`
    - `pytest -k "login"`
    - `pytest --html=report.html`

12. **Як дізнатися який плагін потрібен для команди?**
    - Що станеться якщо запустити команду без встановленого плагіна?

---

**Далі:** `lesson-8-test-output` — аналіз виводу тестів