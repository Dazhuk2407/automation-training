# Питання для самоперевірки — Lesson 38: Custom Exceptions

1. **Навіщо створювати власні винятки замість `ValueError`?**
2. **Як виглядає найпростіший власний виняток?** (`class MyError(Exception): pass`)
3. **Від якого класу треба успадковувати власний виняток?**
4. **Як підняти власний виняток із повідомленням?** (`raise MyError("...")`)
5. **Як отримати текст повідомлення у `except ... as e`?** (`str(e)`)
6. **Що дає ловля базового класу для похідних винятків?**
7. **Чому не варто успадковувати виняток від `BaseException`?**
8. **Як додати винятку кастомний атрибут (напр. код помилки)?**
9. **Навіщо у `__init__` викликати `super().__init__(message)`?**
10. **Де у реальному QA automation корисні `TestDataError` та `ApiResponseError`?**

---

**Далі:** `lesson-39-f-strings`
