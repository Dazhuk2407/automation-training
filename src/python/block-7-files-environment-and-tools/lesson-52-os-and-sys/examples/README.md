# Приклади — Lesson 52: os and sys

- `example_1_env_vars.py` — читання env vars через `os.getenv` з дефолтом
- `example_2_os_paths.py` — `os.path`: basename, dirname, join, splitext
- `example_3_sys_module.py` — `sys`: argv, path, platform, version_info

```bash
pytest example_1_env_vars.py -v
pytest example_2_os_paths.py -v
pytest example_3_sys_module.py -v
```
