# Питання для самоперевірки — Lesson 12: Safe Dictionary Access

## 📋 .get()

1. **Яка різниця між `dict["key"]` та `dict.get("key")`?**
   - Що станеться якщо ключа немає?
   - Коли який спосіб використовувати?

2. **Що повертає `.get("key")` якщо ключа немає і default не задано?**
   - Чому це може бути проблемою при порівнянні?

3. **Чи змінює `.get()` словник?**
   - Що буде зі словником після `config.get("port", 8080)`?

## 🔧 setdefault

4. **Яка різниця між `.get()` та `setdefault()`?**
   - Який з них додає ключ до словника?
   - Коли який використовувати?

5. **Що поверне `config.setdefault("host", "0.0.0.0")` якщо "host" вже є?**
   - Чи зміниться значення?

## 🧪 У тестах

6. **API повертає `{"id": 1, "name": "Alice"}` без email. Як перевірити це в тесті?**
   - Через `.get()` чи через `in`?
   - Який варіант краще і чому?

7. **Як безпечно дістати значення з вкладеного словника?**
   - `response["data"]["user"]["name"]` — чому це небезпечно?
   - Як зробити безпечно?

8. **Чому `assert user.get("email") == "alice@test.com"` впаде якщо email відсутній?**
   - Що порівнюється насправді?

9. **Коли KeyError — це баг, а коли — нормальна ситуація?**
   - Обов'язкові поля vs опціональні
   - Як це впливає на вибір між `[]` та `.get()`?

10. **Як протестувати функцію що приймає config з опціональними полями?**
    - Що має відбуватися при відсутності поля?
    - Як перевірити default значення?

---

**Далі:** `lesson-13-sets`