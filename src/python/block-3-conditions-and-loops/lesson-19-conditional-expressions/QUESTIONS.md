# Питання для самоперевірки — Lesson 19: Conditional Expressions

## 📋 Тернарний оператор

1. **Який синтаксис тернарного оператора?**
   - Як записати `if x > 0: result = "positive" else: result = "negative"` в один рядок?

2. **Коли тернарний оператор доречний, а коли ні?**
   - Наведіть приклад де він покращує читабельність
   - Наведіть приклад де він її погіршує

3. **Чому вкладені тернарні оператори — погана ідея?**

## 🔧 or для defaults

4. **Як працює `a or b` в Python?**
   - Що повертає `"Alice" or "Default"`?
   - Що повертає `"" or "Default"`?
   - Що повертає `0 or 42`?

5. **Чому `port = config.get("port") or 8080` може бути багом?**
   - Коли 0 — валідне значення?
   - Як правильно задати default?

## 🧪 У тестах

6. **Як використати тернарний оператор для format_status()?**

7. **Як `or` допомагає при fallback значеннях?**
   - nickname or name — що це означає?

8. **Коли краще .get(key, default), а коли value or default?**

---

**Далі:** `lesson-20-for-loops`