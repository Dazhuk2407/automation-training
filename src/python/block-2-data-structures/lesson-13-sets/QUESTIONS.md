# Питання для самоперевірки — Lesson 13: Sets

## 📋 Основи

1. **Чим set відрізняється від list?**
   - Чи зберігає set порядок?
   - Чи може set містити дублікати?

2. **Як створити порожню множину?**
   - Чому `{}` не працює?
   - Що повертає `type({})`?

3. **Які типи можуть бути елементами set?**
   - Чи може list бути елементом?
   - А tuple? А dict?
   - Що означає "hashable"?

## 🔧 Операції

4. **Яка різниця між union та intersection?**
   - `A | B` повертає ___
   - `A & B` повертає ___

5. **Що повертає `A - B`?**
   - А `B - A`?
   - Чи це те саме?

6. **Як перевірити що один set — підмножина іншого?**
   - Який метод або оператор?
   - Наведіть приклад з тестами

## 🧪 У тестах

7. **Як перевірити що API повертає всі обов'язкові поля?**
   - Через `issubset` чи через цикл?
   - Який варіант лаконічніший?

8. **Як перевірити що список не містить дублікатів?**
   - Через `len(items) == len(set(items))`
   - Чому це працює?

9. **Як знайти нові елементи порівняно з попередньою версією?**
   - Через `new_set - old_set`
   - Наведіть реальний приклад (endpoints, features)

10. **Чому `assert list(my_set) == [1, 2, 3]` може впасти?**
    - Як правильно порівнювати множини?

---

**Далі:** `lesson-14-mutable-vs-immutable`