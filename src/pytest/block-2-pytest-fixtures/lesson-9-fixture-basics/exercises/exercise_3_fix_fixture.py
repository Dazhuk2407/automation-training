"""
Вправа 3: Виправте падаючий тест.

Фікстура написана правильно — НЕ змінюйте її. Один із тестів навмисно
падає через неправильне очікуване значення. Ваше завдання:

Крок 1: Запустіть файл — один тест падає.
Крок 2: Прочитайте вивід pytest — яке значення насправді у фікстурі?
Крок 3: Заповніть блок ВІДПОВІДЬ унизу.
Крок 4: Виправте assert так, щоб тест проходив.

Запуск: pytest exercise_3_fix_fixture.py -v
"""

import pytest


@pytest.fixture
def account():
    """Готовий рахунок (не змінюйте фікстуру)."""
    return {"owner": "Alice", "balance": 100, "currency": "USD"}


def test_account_owner(account):
    """Цей тест працює правильно."""
    assert account["owner"] == "Alice"


def test_account_balance(account):
    """Цей тест падає — виправте очікуване значення в assert."""
    # TODO: баланс у фікстурі дорівнює 100, а не 999 — виправте число
    assert account["balance"] == 999


def test_account_currency(account):
    """Цей тест працює правильно."""
    assert account["currency"] == "USD"


# ВІДПОВІДЬ:
# Тест, що падав: _______________
# pytest показав, що balance дорівнює: _______________
# Я виправив очікуване значення на: _______________
