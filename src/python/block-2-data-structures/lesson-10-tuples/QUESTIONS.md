# Питання для самоперевірки — Lesson 10: Tuples

## 📋 Основи

1. **Чим tuple відрізняється від list?**
   - Яка головна різниця?
   - Що станеться якщо спробувати змінити tuple?

2. **Як створити tuple з одним елементом?**
   - Чому `(42)` — це не tuple?
   - Що потрібно додати?

3. **Чи можна використовувати tuple як ключ словника?**
   - А list?
   - Чому?

## 🔄 Unpacking

4. **Що таке tuple unpacking?**
   - Як розпакувати `(200, "OK")` у дві змінні?
   - Що означає `_` при unpacking?

5. **Як розпакувати tuple невідомої довжини?**
   - Що робить `*` при unpacking?
   - Що повертає `first, *rest = (1, 2, 3, 4)`?

6. **Що станеться якщо кількість змінних не відповідає tuple?**
   - Яка помилка виникне?

## 🧪 У тестах

7. **Навіщо функції повертають tuple?**
   - Наведіть приклад де це зручно
   - Як це використати в тесті?

8. **Як pytest.mark.parametrize використовує tuples?**
   - Який формат даних приймає?
   - Як розпаковуються параметри?

9. **Коли в тестах краще tuple, а коли list?**
   - Якщо дані фіксовані (test cases) — що обрати?
   - Якщо дані змінюються (результат фільтрації) — що обрати?

10. **Що таке namedtuple і чим він кращий за звичайний tuple?**
    - Яку проблему вирішує?
    - Чи можна звертатися за індексом?

---

**Далі:** `lesson-11-dictionaries`