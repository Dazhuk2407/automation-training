# Питання для самоперевірки — Lesson 41: Working with JSON Data

1. **Що таке JSON і чому його важливо знати в QA automation?**
2. **Що робить `json.loads()` і що повертає?**
3. **Що робить `json.dumps()` і що повертає?**
4. **Як запамʼятати різницю між `loads` та `dumps`?**
5. **У який Python-тип перетворюється JSON `object`, а в який — `array`?**
6. **У що перетворюються JSON `true`, `false`, `null`?**
7. **Навіщо потрібні параметри `indent`, `sort_keys`, `ensure_ascii`?**
8. **Як безпечно дістати вкладене поле з API-відповіді, не ризикуючи `KeyError`?**
9. **Який виняток кидає `json.loads()` на невалідному рядку і як його обробити?**
10. **Чому `json.loads("{'name': 'Alice'}")` викине помилку?**

---

**Далі:** `lesson-42-classes-and-objects`
