# Питання для самоперевірки — Lesson 53: Dates and Time

1. **Чим відрізняються класи `date`, `datetime` і `timedelta`?**
2. **Як створити конкретну дату й час, наприклад 15 січня 2024, 10:30?**
3. **Чому `datetime.now()` не можна порівнювати з фіксованим значенням у тестах?**
4. **Які атрибути має `datetime` і чим `weekday()` відрізняється від `year`?**
5. **Що робить `strftime` і в який бік конвертує (дата→рядок чи рядок→дата)?**
6. **Що робить `strptime` і в який бік конвертує?**
7. **Що означають коди `%Y`, `%m`, `%d`, `%H`, `%M`, `%S`?**
8. **Як отримати кількість днів між двома датами?** (`(d2 - d1).days`)
9. **Як додати 7 днів до дати?** (`dt + timedelta(days=7)`)
10. **Де у реальному QA automation використовуються timestamps?**

---

**Далі:** `lesson-54-random-data`
