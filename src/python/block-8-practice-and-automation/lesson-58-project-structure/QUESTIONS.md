# Питання для самоперевірки — Lesson 58: Project Structure

1. **Навіщо проєкту структура, а не один великий файл?**
2. **Що зазвичай лежить у теці `src/` та у теці `tests/`?**
3. **Для чого потрібен файл `requirements.txt`?**
4. **Що описують у `.gitignore` і чому не варто комітити `venv/`?**
5. **Яку роль виконує `conftest.py` у pytest?**
6. **Навіщо `tests/` тримають окремо від коду застосунку?**
7. **Що означає «дзеркалення» структури тестів і коду?**
8. **За що відповідають теки `pages/`, `api/`, `helpers/`, `data/` у QA-фреймворку?**
9. **Навіщо у теці потрібен файл `__init__.py`?**
10. **Які правила найменування файлів у Python-проєкті (snake_case, `test_`)?**

---

**Далі:** `lesson-59-cli-arguments`
