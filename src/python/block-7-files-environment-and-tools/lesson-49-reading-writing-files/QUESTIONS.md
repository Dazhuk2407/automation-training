# Питання для самоперевірки — Lesson 49: Reading and Writing Files

1. **Навіщо використовувати `with open(...)` замість простого `open()`?**
2. **Що робить контекстний менеджер, коли блок `with` завершується?**
3. **Чим відрізняється режим `"w"` від режиму `"a"`?**
4. **Що станеться з наявним вмістом файлу, якщо відкрити його в режимі `"w"`?**
5. **У чому різниця між `read()`, `readlines()` та ітерацією `for line in f`?**
6. **Чому важливо вказувати `encoding="utf-8"`?**
7. **Навіщо викликати `.strip()` під час читання рядків?**
8. **Чи додає `write()` символ `\n` автоматично?**
9. **Який режим використати для лог-файлу, щоб не втратити попередні записи?**
10. **Де у реальному QA automation читають/пишуть файли?**

---

**Далі:** `lesson-50-csv-and-json-files`
