"""
Вправа 3: виправте неправильний scope.

Фікстура cart має scope="module" — це ОДИН список на весь файл.
Тест test_isolation очікує свіжий порожній кошик, але отримує стан,
який залишив test_add. Через це test_isolation ПАДАЄ.

Крок 1: Запустіть файл — рівно один тест падає.
Крок 2: Зрозумійте причину: широкий scope + мутація спільного стану.
Крок 3: Виправте scope фікстури так, щоб кожен тест отримував свіжий кошик.
Крок 4: Заповніть блок ВІДПОВІДЬ.

Запуск: pytest exercise_3_fix_scope.py -v
"""

import pytest


@pytest.fixture(scope="module")  # ❌ неправильний scope ламає ізоляцію
def cart():
    """Має давати КОЖНОМУ тесту свіжий порожній кошик."""
    return []


def test_add(cart):
    """Додаємо товар — у кошику 1 елемент."""
    cart.append("apple")
    assert len(cart) == 1


def test_isolation(cart):
    """Очікуємо свіжий порожній кошик (падає через module scope)."""
    assert cart == []


def test_is_list(cart):
    """Кошик — це список (проходить за будь-якого scope)."""
    assert isinstance(cart, list)


# ВІДПОВІДЬ:
# Причина падіння: _______________
# Що виправити: _______________
#
# Правильний варіант — прибрати широкий scope, щоб фікстура була function
# scope (за замовчуванням) і створювалася заново для кожного тесту:
#
#     @pytest.fixture
#     def cart():
#         return []
