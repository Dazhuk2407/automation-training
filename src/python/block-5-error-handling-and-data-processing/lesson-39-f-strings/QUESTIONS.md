# Питання для самоперевірки — Lesson 39: f-strings

1. **Що таке f-string і чим він відрізняється від звичайного рядка?**
2. **Чому f-strings кращі за `.format()` та `%`?**
3. **Які вирази можна писати всередині `{}`?** (арифметика, виклики методів, доступ до dict)
4. **Що виведе `f"{19.5:.2f}"`?** (`"19.50"`)
5. **Як додати роздільник тисяч до числа?** (`f"{n:,}"`)
6. **Що робить специфікатор `:03d`?** (доповнює нулями зліва до ширини 3)
7. **Як вивести число як відсоток з одним знаком?** (`f"{ratio:.1%}"`)
8. **Чим відрізняються `:<10`, `:>10` та `:^10`?**
9. **Що виведе `f"{x=}"` якщо `x = 5`?** (`"x=5"`)
10. **Яка типова помилка з лапками всередині f-string і як її уникнути?**

---

**Далі:** `lesson-40-regular-expressions`
