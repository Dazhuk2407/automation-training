# Питання для самоперевірки — Lesson 54: Random Data

1. **Чим `randint(1, 6)` відрізняється від `range(1, 6)`?**
2. **Що повертає `random.random()` і в якому діапазоні?**
3. **Яка різниця між `choices` і `sample`?**
4. **Що повертає `random.shuffle(list)`?**
5. **Навіщо потрібен `random.seed()`?**
6. **Чому seed краще ставити всередині тесту, а не глобально?**
7. **Що станеться, якщо `k` у `sample` більший за розмір послідовності?**
8. **Чому для паролів/токенів не можна використовувати `random`?**
9. **Який модуль потрібен для криптостійких секретів?**
10. **Як згенерувати випадковий валідний емейл для тесту?**

---

**Далі:** `lesson-55-modules-and-packages`
