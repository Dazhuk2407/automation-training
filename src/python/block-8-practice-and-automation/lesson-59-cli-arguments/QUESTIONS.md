# Питання для самоперевірки — Lesson 59: CLI Arguments

1. **Чому argparse кращий за ручний розбір `sys.argv`?**
2. **Які три кроки потрібні для базового використання argparse?**
3. **Чим позиційний аргумент відрізняється від опції (`--flag`)?**
4. **Що робить `type=int` в `add_argument`?**
5. **Навіщо параметр `choices` і що станеться при значенні поза набором?**
6. **Як зробити опцію обовʼязковою?** (`required=True`)
7. **Що робить `action="store_true"` і яке значення за замовчуванням?**
8. **Як отримати значення після `parse_args()`?**
9. **У який атрибут перетвориться опція `--dry-run`?** (`args.dry_run`)
10. **Чому побудову parser у QA-раннері виносять в окрему функцію `build_parser()`?**

---

**Далі:** `lesson-60-file-paths`
