# Питання для самоперевірки — Lesson 6: Assertions

## 🧠 Assert Introspection

1. **Як pytest показує помилки при порівнянні словників?**
   - Що побачите у виводі якщо два словники відрізняються одним ключем?
   - Чому це краще за `assertEqual` з unittest?

2. **Чи потрібно писати `assert x == 5, f"Expected 5 got {x}"` у pytest?**
   - Що pytest покаже і без message?
   - Коли message все-таки корисний?

## ✅ Правильний стиль

3. **Чому `assert condition` краще за `assert condition is True`?**
   - У чому різниця?
   - Коли `is` доречний? (підказка: None)

4. **Чому `isinstance(x, int)` краще за `type(x) == int`?**
   - Що станеться з підкласами?
   - Наведіть приклад коли `type()` дасть неправильний результат

5. **Яка різниця між `==` та `is`?**
   - `a == b` перевіряє _______
   - `a is b` перевіряє _______
   - Коли використовувати яке?

## 🔍 Винятки

6. **Як перевірити що функція кидає конкретний виняток?**
   - Який синтаксис у `pytest.raises`?
   - Що станеться якщо виняток НЕ виникне?

7. **Що робить параметр `match` у `pytest.raises`?**
   - Що він приймає — рядок чи regex?
   - Наведіть приклад

## 💡 Практика

8. **Чому "один тест — одна ідея" — це важливо?**
   - Що станеться якщо перший assert впаде?
   - Скільки інформації ви втратите?

9. **Коли assert message дійсно потрібен?**
   - Наведіть приклад де message додає цінність
   - Наведіть приклад де message зайвий

10. **Дано тест з 8 assert. Як його покращити?**
    - Як розбити на кілька тестів?
    - Яка перевага окремих тестів при дебагу?

---

**Далі:** `lesson-7-run-tests` — запуск тестів з командного рядка