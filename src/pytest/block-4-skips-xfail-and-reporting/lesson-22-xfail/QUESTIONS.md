# Питання для самоперевірки — Lesson 22: xfail

## 🧠 Основи xfail

1. **Що означає `xfail` і коли його використовувати?**
   - Розшифруйте абревіатуру.
   - Наведіть типовий QA-сценарій.

2. **Чи виконується тіло тесту, позначеного `@pytest.mark.xfail`?**
   - Порівняйте зі `skip`.
   - Що станеться з кодом тесту?

3. **Чому `reason` важливий у `xfail`?**
   - Що ви туди зазвичай пишете?
   - Чим поганий `@pytest.mark.xfail` без reason?

## ✅ Статуси

4. **Яка різниця між `xfailed` і `xpassed`?**
   - Коли тест отримує статус `xfailed`?
   - Коли — `xpassed`?

5. **Чи є `xfailed` падінням (failure)?**
   - Який exit code буде у pytest?
   - Скільки `failed` покаже підсумок?

6. **Що зазвичай означає `xpassed`?**
   - Яка найімовірніша причина?
   - Що варто зробити далі?

## 🔒 strict

7. **Що робить `strict=True`?**
   - Як зміниться поведінка при `xpass`?
   - Навіщо це QA-команді?

8. **Тест зі `strict=True` пройшов. Який статус у звіті і який exit code?**
   - Що потрібно зробити з таким тестом?

## 🔀 Умовний xfail і порівняння

9. **Як позначити тест як xfail лише на Windows?**
   - Який синтаксис умовного xfail?
   - Що буде на інших ОС?

10. **Коли обрати `skip`, а коли `xfail`?**
    - Фіча ще не написана — що використати?
    - Є баг у наявному коді — що використати?

---

**Далі:** `lesson-23-last-failed`
