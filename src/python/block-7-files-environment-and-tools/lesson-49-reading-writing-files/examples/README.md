# Приклади — Lesson 49: Reading and Writing Files

- `example_1_write_read.py` — запис і читання через `with open()`
- `example_2_modes.py` — режими `"w"`, `"a"`, `"r"` та encoding
- `example_3_lines.py` — читання рядків, ітерація, `strip()`

```bash
pytest example_1_write_read.py -v
pytest example_2_modes.py -v
pytest example_3_lines.py -v
```
