# Питання для самоперевірки — Lesson 13: conftest.py

## 🧠 Призначення conftest.py

1. **Навіщо потрібен `conftest.py`?**
   - Яку проблему він вирішує?
   - Що було б, якби спільні фікстури лежали в кожному файлі окремо?

2. **Чи треба імпортувати фікстуру з `conftest.py`?**
   - Як тест отримує доступ до фікстури з conftest?
   - Що станеться, якщо написати `from conftest import sample_user`?

## 📍 Область дії

3. **На які тести діє `conftest.py`?**
   - Тека, підтеки, сусідні теки — де фікстури видно, а де ні?
   - Чи побачить тест `conftest.py`, що лежить у батьківській теці?

4. **Скільки файлів `conftest.py` може бачити один тест?**
   - Що станеться, якщо `conftest.py` є і в корені, і в підтеці?
   - Який шлях по дереву pytest враховує?

5. **Куди покласти `conftest.py`, щоб його бачили всі тести теки `tests/`?**
   - Чи спрацює `conftest.py` збоку від теки з тестами?

## 🔁 Перекриття (override)

6. **Що станеться, якщо у тестовому файлі оголосити фікстуру з тим самим ім'ям, що й у conftest?**
   - Яка версія виграє?
   - Як це назвати одним словом?

7. **Коли override фікстури корисний?**
   - Наведіть приклад, де більшості тестів підходить conftest-фікстура, а одному — ні.

## 💡 Практика (QA)

8. **Які типові фікстури виносять у `conftest.py` на реальних проєктах?**
   - Наведіть 2-3 приклади (підказка: client, config, test_data).

9. **Фікстура `client` залежить від `config`. Чи можна тримати обидві в conftest?**
   - Як одна фікстура conftest використовує іншу?

10. **У вас три тестові файли з однаковою скопійованою фікстурою. Як це виправити?**
    - Куди її перенести?
    - Що зміниться в самих тестових файлах?

---

**Далі:** `lesson-14-fixture-best-practices`
