# Питання для самоперевірки — Lesson 46: Inheritance Basics

1. **Як синтаксично оголосити підклас від батьківського класу?** (`class Child(Parent):`)
2. **Що успадковує підклас з тілом `pass`?**
3. **Навіщо викликати `super().__init__()` у конструкторі підкласу?**
4. **Що станеться з атрибутами батька, якщо забути `super().__init__()`?**
5. **Що таке override (перевизначення) методу?**
6. **Як з перевизначеного методу викликати батьківську версію?** (`super().method()`)
7. **Чим `isinstance(obj, Class)` відрізняється від `issubclass(Child, Parent)`?**
8. **Чи поверне `isinstance(admin, User)` значення `True`, якщо `Admin(User)`?**
9. **Чим override відрізняється від overload у Python?**
10. **Де у реальному QA automation застосовують наслідування?** (`BaseTest`, `BasePage`)

---

**Далі:** `lesson-47-oop-concepts`
