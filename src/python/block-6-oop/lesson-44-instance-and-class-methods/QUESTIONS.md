# Питання для самоперевірки — Lesson 44: Instance and Class Methods

1. **Що таке метод екземпляра і що означає параметр `self`?**
2. **Чим атрибут класу відрізняється від атрибута екземпляра?**
3. **Де оголошується атрибут класу, а де атрибут екземпляра?**
4. **Що робить декоратор `@classmethod` і що таке `cls`?**
5. **Як викликати `@classmethod` без створення екземпляра?**
6. **Що таке classmethod-фабрика (альтернативний конструктор)?**
7. **Чому у factory повертають `cls(...)`, а не `ClassName(...)`?**
8. **Чому небезпечно мутувати атрибут класу через екземпляр?**
9. **Що станеться, якщо забути декоратор `@classmethod`?**
10. **Де у реальному QA automation використовують `from_dict` і лічильник?**

---

**Далі:** `lesson-45-static-methods`
