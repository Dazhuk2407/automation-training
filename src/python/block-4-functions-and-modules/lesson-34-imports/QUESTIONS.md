# Питання для самоперевірки — Lesson 34: Imports

1. **Чим `import os` відрізняється від `from os import path`?**
2. **Коли використовувати alias (as)?**
3. **Чому `import *` — погана практика?**
4. **Який порядок імпортів рекомендує PEP 8?**
5. **Що таке circular import і як його уникнути?**
6. **Чому невикористані імпорти — проблема?**
7. **Як імпортувати модуль з src/ у тестах?**
8. **Чим `import json` краще за `from json import *`?**
9. **Коли `from module import func` зручніше за `import module`?**

---

**Вітаємо! Ви завершили Block 4.**