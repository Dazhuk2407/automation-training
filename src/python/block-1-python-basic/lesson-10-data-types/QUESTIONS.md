# Питання для самоперевірки - Lesson 10: Data types

## 🎯 Типи даних

1. **Назвіть 5 основних типів даних Python**
   - Для кожного наведіть приклад значення

2. **Чим `int` відрізняється від `float`?**
   - Що поверне `type(1)`? А `type(1.0)`?
   - Чому `1 == 1.0` дає `True`, хоча типи різні?

3. **Що таке `None`?**
   - Чим `None` відрізняється від `0`, `""`, `False`?
   - Коли функція повертає `None`?
   - Як правильно перевірити на None: `== None` чи `is None`?

## 🔍 Перевірка типів

4. **`type()` vs `isinstance()`**
   - Чим вони відрізняються?
   - Чому `isinstance(True, int)` повертає `True`?
   - Коли краще використовувати `isinstance()`?

## 🔄 Конверсія типів

5. **Конверсія в int**
   - Що поверне `int(3.99)`? Чому не 4?
   - Що поверне `int(-2.7)`? Чому не -3?
   - Чому `int("3.14")` викликає помилку, а `int(3.14)` — ні?

6. **Конверсія в bool (truthy/falsy)**
   - Які значення є falsy в Python? (Перелічіть мінімум 5)
   - Чому `bool("False")` дає `True`?
   - Чому `bool(" ")` дає `True`?
   - Що поверне `bool([])`? А `bool([0])`?

7. **Обробка помилок конверсії**
   - Що станеться при `int("abc")`?
   - Як написати безпечну конверсію?
   - Різниця між `ValueError` та `TypeError`?

## ✅ Практичні завдання

8. **Визначте результат без запуску:**
   ```python
   a = "42"
   b = int(a)
   c = float(a)
   print(type(a), type(b), type(c))
   ```

9. **Знайдіть помилку:**
   ```python
   age = input("Enter age: ")  # користувач вводить "25"
   next_year = age + 1
   ```

10. **Що виведе цей код?**
    ```python
    print(bool(0), bool("0"), bool(""), bool(None))
    ```

---

**✅ Коли типи даних зрозумілі — переходьте до Lesson 11**