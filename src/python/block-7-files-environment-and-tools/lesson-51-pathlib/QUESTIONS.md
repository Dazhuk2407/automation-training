# Питання для самоперевірки — Lesson 51: pathlib

1. **Які переваги `pathlib` над рядковими шляхами та `os.path`?**
2. **Як створити шлях і з'єднати його частини?** (`Path("data") / "logs" / "app.log"`)
3. **Для чого потрібен `Path(__file__)` у тестах?**
4. **У чому різниця між `.name` та `.stem`?**
5. **Що повертає `.suffix` для `Path("report.csv")`?** (`".csv"` — з крапкою)
6. **Чим відрізняються `.exists()`, `.is_file()` та `.is_dir()`?**
7. **Як прочитати й записати текст через `Path`?** (`read_text` / `write_text`)
8. **Навіщо `parents=True` та `exist_ok=True` у `mkdir`?**
9. **Яка різниця між `glob("*.txt")` та `rglob("*.txt")`?**
10. **Як зібрати всі `.json` файли з теки тест-даних?**

---

**Далі:** `lesson-52-os-and-sys`
