# Питання для самоперевірки — Lesson 22: break and continue

1. **Яка різниця між break та continue?**
2. **Що станеться з рештою ітерацій після break?**
3. **Коли else у циклі виконується, а коли ні?**
4. **Чому `break` замість `continue` — це різні речі?**
5. **Як через break знайти перший елемент що відповідає умові?**
6. **Як через continue пропустити невалідні дані?**
7. **Чим `return` у циклі відрізняється від `break`?**
8. **Чи виконається else для порожнього циклу `for item in []:`?**

---

**Далі:** `lesson-23-list-comprehensions`