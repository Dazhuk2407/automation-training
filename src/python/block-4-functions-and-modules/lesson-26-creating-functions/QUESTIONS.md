# Питання для самоперевірки — Lesson 26: Creating Functions

1. **Навіщо потрібні функції?**
   - Як вони допомагають уникнути дублювання?
   - Як впливають на читабельність?

2. **Що повертає функція без `return`?**

3. **Чим параметр відрізняється від аргументу?**

4. **Що станеться якщо викликати функцію без дужок?**
   - `result = get_status` vs `result = get_status()`

5. **Як правильно називати функції в Python?**
   - Який стиль: snake_case чи CamelCase?
   - Які префікси для boolean функцій?

6. **Чому `def list(items):` — небезпечно?**

7. **Як написати helper-функцію для валідації у тестах?**

8. **Що станеться якщо визначити функцію після її виклику?**

9. **Яка перевага `is_valid_email(x)` над `len(x) > 0 and "@" in x`?**

10. **Чи може функція викликати іншу функцію?**

---

**Далі:** `lesson-27-arguments`