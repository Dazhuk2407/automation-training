# Питання для самоперевірки — Lesson 9: Lists

## 📋 Основи

1. **Як створити порожній список?**
   - Два способи: `[]` та `list()`
   - Який спосіб краще і чому?

2. **Що повертає `items[-1]`?**
   - А `items[-2]`?
   - Що станеться якщо список порожній?

3. **Чим `items[1:3]` відрізняється від `items[1:4]`?**
   - Чи включається елемент за індексом stop?

## 🔧 Методи

4. **Яка різниця між `append()` та `extend()`?**
   - Що станеться з `items.append([4, 5])`?
   - А з `items.extend([4, 5])`?

5. **Яка різниця між `sorted()` та `.sort()`?**
   - Що повертає `.sort()`?
   - Чому `result = data.sort()` — це баг?

6. **Яка різниця між `remove()` та `pop()`?**
   - Що приймає кожен метод?
   - Що повертає?

## 🧪 У тестах

7. **Як перевірити що список не порожній?**
   - Через `len()` чи через truthiness?
   - Який спосіб ідіоматичний у Python?

8. **Чому видалення елементів під час ітерації — небезпечне?**
   - Що станеться з індексами?
   - Яка альтернатива?

9. **Як перевірити що всі елементи списку відповідають умові?**
   - Підказка: `all()` або цикл з assert

10. **Чому у тесті краще створювати свіжий список, а не змінювати існуючий?**
    - Як мутація списку може зламати інші тести?

---

**Далі:** `lesson-10-tuples`