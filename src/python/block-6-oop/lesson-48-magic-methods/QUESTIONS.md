# Питання для самоперевірки — Lesson 48: Magic Methods

1. **Що таке dunder / magic методи і хто їх викликає?**
2. **Яку різницю між `__str__` і `__repr__`?**
3. **Який метод викликається, коли об'єкт знаходиться всередині списку `[obj]`?**
4. **Що станеться при `==`, якщо не визначити `__eq__`?**
5. **Як `__eq__` порівнює об'єкти за значенням?**
6. **Що робить `__len__` і що він мусить повертати?**
7. **Який dunder-метод відповідає за оператор `in`?**
8. **Чому після визначення `__eq__` об'єкт може стати unhashable?**
9. **Чому `__repr__` корисний при падінні assert у тестах?**
10. **Як `__eq__` спрощує asserts у QA automation?**

---

**Далі:** `lesson-49-reading-writing-files`
