# Питання для самоперевірки — Lesson 5: Прості тести для базових типів

## 🔢 Числа

1. **Чому `assert 0.1 + 0.2 == 0.3` може впасти?**
   - Що таке float precision?
   - Як правильно порівнювати float у pytest?

2. **Який результат `10 // 3` і `10 % 3`?**
   - Чим `//` відрізняється від `/`?

3. **Чи потрібен `pytest.approx` для цілих чисел?**
   - Коли він потрібен, а коли — ні?

## 📝 Рядки

4. **Чому `assert "Hello" == "hello"` впаде?**
   - Як зробити порівняння без урахування регістру?

5. **Яка різниця між `in` для рядка та `in` для списку?**
   - `"test" in "pytest"` — що перевіряється?
   - `"test" in ["pytest"]` — що перевіряється?
   - Чому результати різні?

6. **Чим `startswith` краще за слайс для перевірки початку рядка?**
   - Що читабельніше: `url[:8] == "https://"` чи `url.startswith("https://")`?

## 📊 Колекції

7. **Чому не можна покладатися на порядок елементів у `set`?**
   - Що станеться з `assert list({3, 1, 2}) == [1, 2, 3]`?
   - Як правильно порівнювати множини?

8. **Чим `user["name"]` відрізняється від `user.get("name")`?**
   - Що станеться якщо ключа немає?
   - Коли використовувати `.get()`?

9. **Чому порожні колекції вважаються `False`?**
   - Що поверне `bool([])`?
   - Як це використовувати в тестах?

10. **Чи можна сортувати список зі змішаними типами?**
    - Що станеться з `sorted([1, "a", 2])`?
    - Чому Python 3 забороняє це?

## 💡 Практика

11. **Чому один великий `test_everything()` — погана ідея?**
    - Що станеться якщо третій assert з п'яти впаде?
    - Скільки інформації ви втратите?

12. **Чому мутація списку всередині тесту небезпечна?**
    - Що станеться з перевірками після `items.append(4)`?
    - Як уникнути цієї проблеми?

13. **Як перевірити що список відсортований?**
    - Назвіть два способи

---

**Далі:** `lesson-6-assertions` — детальніше про assertions